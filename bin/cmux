#!/usr/bin/env bash
set -euo pipefail
cat >&2 <<'MSG'
This repository is the MBP tmux workspace monitor, not native cmux.

Use:
  tmux-workbench
  cockpit-workbench
  mbp-cockpit-tmux

Native tmux-workbench belongs on the iMac and should come from manaflow-ai/cmux.
MSG
exit 64
